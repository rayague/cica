name: Deploy to Hostinger via FTP

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 37.44.246.216
          username: ${{ secrets.u397987894 }}
          password: ${{ secrets.Cicapressing00@ }}
          port: 21
          protocol: ftp
          server-dir: cica/